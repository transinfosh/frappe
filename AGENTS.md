# Frappe 核心应用代理执行说明

## 适用范围

本文档适用于 `/workspace/development/frappe-bench/apps/frappe` 目录下的所有代码、文档、测试与脚本改动。

代理在处理本应用相关任务时，应将本文档视为执行说明。

## 规则优先级

- 如果更深层目录中存在更具体的规则文件，则以“离目标文件更近”的规则为准。
- 当局部现有实现方式与通用偏好不一致，且任务未明确要求统一重构时，优先延续目标文件及同目录的既有写法。

## 应用上下文

- 这是 Frappe 框架核心应用，改动可能影响多个业务 app、公共 API、前端框架行为、数据库模型和系统级功能。
- 日常改动通常会落在以下区域：
  - `frappe/frappe/*`
  - `frappe/public/*`
  - `frappe/www/*`
  - `frappe/desk/*`
  - `frappe/core/*`

## 执行原则

- 对框架核心改动保持保守，优先考虑兼容性、回归风险和影响范围。
- 优先做最小必要改动，不为了局部任务顺手重构公共底层模块。
- 修改前，先确认该逻辑是否属于公共 API、公共前端行为、系统设置、权限、数据库查询或事务管理的一部分。

## 需要尊重的现有模式

- 处理核心框架代码时，应优先匹配当前模块、相邻文件和同层目录的既有实现方式。
- 不要因为个人风格偏好改变成熟公共模块的组织形式、命名习惯或接口结构。
- 对 `public`、`desk`、`core`、`model`、`database` 等公共区域的改动，应默认视为高影响范围改动。

## 变更纪律

- 不要在完成当前需求的同时夹带无关清理或大范围重排。
- 不要未经要求修改公共 API、白名单接口签名、返回结构、事件契约、数据库字段语义或系统级默认行为。
- 不要修改代码中的 auto-generated 区块，除非任务明确要求且确认该改动不会被后续重新生成覆盖。
- 涉及权限、事务、缓存、队列、模型元数据、表单脚本底层行为时，应优先保持兼容性。
- 涉及 patch、迁移、数据修复或初始化逻辑时，应优先保持幂等，避免重复执行产生副作用。
- 如果改动可能影响多个 app 或公共运行时行为，应在实现和总结中明确指出影响范围。

## 验证要求

- 修改完成后，尽量执行与改动范围最匹配、最小化的验证。
- 对核心框架逻辑、公共 API、系统设置、权限或数据库相关改动，应优先进行更严格的验证。
- 如果本次无法完成有效验证，必须在最终说明中明确指出。

## 代理输出要求

- 对框架核心改动，应在总结中简要说明影响范围与兼容性考虑。
- 如果本次改动涉及公共行为或潜在回归风险，应明确指出剩余风险。
- 最终总结保持简洁，重点说明行为变化、验证结果与剩余风险。
